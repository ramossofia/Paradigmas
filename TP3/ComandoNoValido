package Explorer;

public class ComandoNoValido extends Comando {
    private final char comando;

    public ComandoNoValido(char comando) {
        this.comando = comando;
    }

    @Override
    public void ejecutar(Explorer explorer)  {
        throw new RuntimeException("Comando no válido: " + comando);
    }
}
